# T2.6 — XREAL One Pro bring-up implementation checklist

> Self-contained implementation plan for the XREAL Unity port. Produced 2026-05-08 from a `/grill-with-docs` session that resolved the major architectural decisions. Hand to an AI coding agent or work through manually.
>
> **Read first:** `../../CONTEXT.md` (verified SDK surface, resolved decisions, glossary), `../adr/0001-opencv-bridging-staircase.md` (this is also at `.docs/adr/...` for users with both folders, but the canonical version is in `docs/`). The Siemens-confidential `.docs/PROJECT.md`, `.docs/TASKS.md`, and `.docs/THEORY.md` are useful when available but are gitignored, so an AI agent cloning the public repo will not see them — the checklist is therefore self-contained.
>
> **Do not** copy code from the archived `.docs/.archive/T2.6-xreal-port.OUTDATED.md` — its snippets target the legacy NRSDK (`NRSessionManager`, `NRRGBCamTexture`, `NRHMDPoseTracker`, `NRFrame`) and OpenCvSharp, neither of which match the installed `com.xreal.xr@3.1.0`.

## Architecture summary

```
                      ┌──────────────────────────────────┐
                      │   EyeLab Service (Python)        │
                      │   on Testlab PC or Surface Pro 7 │
                      │   - WebSocket server :8765       │
                      │   - Extends webcam_pipeline.py   │
                      │   - cv2.aruco + solvePnP         │
                      │   - Per-client calibration YAML  │
                      └──────────┬─────────┬─────────────┘
                                 │         │
                       binary    │         │   JSON pose
                       JPEG Y    │         │   downlink
                       uplink    │         │
                                 ▼         ▲
                      ┌──────────────────────────────────┐
                      │   Unity App                      │
                      │   - IArucoPoseBridge             │
                      │     (WebSocket / Mock / future   │
                      │      Native / OpenCVForUnity)    │
                      │   - IFrameSource                 │
                      │     (Editor=WebCamTexture        │
                      │      Android=XREALRGBCameraTex)  │
                      │   - HeadPoseHistory ringbuffer   │
                      │   - StructureAnchor (GameObject  │
                      │     + EWMA + outlier reject)     │
                      │   - GeometryRenderer (wireframe) │
                      └──────────────────────────────────┘
```

## Conventions

- **Coordinate frames:** `OpenCV` (right-handed, +Y down, +Z forward) → `Unity` (left-handed, +Y up, +Z forward). All conversions go through one utility class `CoordinateConverter` (THEORY.md §6.3). Never duplicate the conversion.
- **Units:** metres, throughout. UNV is metres. Camera intrinsics in pixels.
- **Time:** nanoseconds, monotonic clock. `XREALPlugin.TryGetRGBCameraFrame(ref ulong)` returns ns; the Python service echoes the frame timestamp in every pose response.
- **Threading:** WebSocket I/O on a background task; pose results posted to the Unity main thread via `XREALMainThreadDispatcher.PerformActionInUpdate()` (already in the SDK).

## Repo layout after this work

```
EyeLab/
├── CONTEXT.md
├── .docs/
│   ├── PROJECT.md  THEORY.md  TASKS.md
│   ├── adr/0001-opencv-bridging-staircase.md
│   └── tasks/T2.6-implementation-checklist.md   ← this file
├── python/
│   ├── webcam_pipeline.py            (existing)
│   ├── eyelab_service.py             (NEW — Phase 1)
│   ├── handshake.py                  (NEW — Phase 1)
│   ├── frame_protocol.py             (NEW — Phase 1)
│   ├── calibrations/
│   │   ├── unity-editor-webcam-default.yml
│   │   └── unity-android-xreal-eye-default.yml   (filled in Phase 6)
│   └── tests/
│       └── test_handshake.py         (NEW — Phase 1)
└── eyelab_xreal/
   Assets/
   ├── EyeLab/
   │   ├── Bridge/
   │   │   ├── IArucoPoseBridge.cs
   │   │   ├── PoseResult.cs
   │   │   ├── WebSocketPoseBridge.cs
   │   │   ├── MockPoseBridge.cs
   │   │   └── BridgeConfig.asset    (ScriptableObject)
   │   ├── Frames/
   │   │   ├── IFrameSource.cs
   │   │   ├── WebCamFrameSource.cs       (Editor/desktop)
   │   │   └── XREALEyeFrameSource.cs     (Android)
   │   ├── Pose/
   │   │   ├── HeadPoseHistory.cs
   │   │   ├── StructureAnchor.cs
   │   │   └── CoordinateConverter.cs
   │   ├── Geometry/
   │   │   ├── WireframeData.cs           (matches python's JSON schema)
   │   │   ├── WireframeLoader.cs
   │   │   ├── GeometryRenderer.cs
   │   │   └── NodeStatusController.cs
   │   ├── Calibration/
   │   │   ├── CalibrationFlow.cs
   │   │   └── SpaamFlow.cs               (Phase 8)
   │   └── Scenes/
   │       ├── 00_Bringup.unity           (smoke test)
   │       ├── 10_Calibration.unity
   │       └── 20_EMA.unity               (the actual app)
```

---

## Phase 0 — project hygiene (~1 day)

**Goal:** clean baseline before any new code.

- [ ] Decide fate of `unity_projects/My project/` (MR Template, OpenXR-based). **Recommended: rename to `My project (archived OpenXR experiment)`** and stop touching it; do not delete in case it's useful for the WP5 Sony port.
- [x] In `eyelab_xreal/Assets/`, create the `EyeLab/` folder structure shown above. Empty `.gitkeep` files in each leaf are fine for now.
- [x] In `Packages/manifest.json`, ensure these are present (some likely already from the URP template; add the AR Foundation ones if missing):
  - `com.unity.xr.management`
  - `com.unity.xr.arfoundation` (>= 6.0)
  - `com.unity.xr.core-utils`
  - `com.unity.nuget.newtonsoft-json` (for JSON parsing)
- [x] Add a WebSocket library. Recommendation: [`NativeWebSocket`](https://github.com/endel/NativeWebSocket) — small, MIT, IL2CPP-compatible. Add via UPM Git URL in `manifest.json`:
  `"com.endel.nativewebsocket": "https://github.com/endel/NativeWebSocket.git#upm"`
- [x] Keep `eyelab_xreal/Packages/com.xreal.xr/` local unless redistribution is explicitly cleared. The SDK is publicly downloadable from XREAL, but the local package has no license file granting redistribution of modified SDK binaries. Git tracks `manifest.json`, `packages-lock.json`, `Assets/`, `ProjectSettings/`, and local patch scripts, not the unpacked SDK bundle.
- [x] In `Project Settings → XR Plug-in Management`:
  - Android tab → enable XREAL XR Loader.
  - PC tab → leave XR off (Editor uses webcam, not the glasses on PC).
- [x] In `Player Settings → Android`: Min API 28, Target 34, ARM64 only, IL2CPP, Linear color space.
- [x] Confirm the `AndroidManifest.xml` does **not** include `com.nreal.intent.action.LAUNCH` or any Beam-Pro-specific tags (T2.6 §7 had these — drop them).

**Done when:** Unity project opens cleanly, builds an empty APK on Android target without errors, and the EyeLab folder structure exists.

**Status 2026-05-11:** Phase 0 gate passed. Empty Android APK build succeeded in Unity 6000.4.5f1 after patching the local, gitignored XREAL SDK copy so `nr_common.aar` uses manifest package `nrsdk.pack.common` instead of duplicating `nr_loader.aar`'s `nrsdk.pack`. Reapply on other machines with `tools/patch_xreal_sdk.ps1`; the script keeps the original local vendor file as `eyelab_xreal/Packages/com.xreal.xr/Runtime/Plugins/Android/nr_common.aar.original`. Build emitted vendor-library warnings that `libXREALXRPlugin.so`, `libmedia_codec.so`, and `libVulkanSupport.so` are not 16 KB page-size aligned for some Android 15+ ARM64 devices; track this as an SDK/device-compatibility risk, not a Phase 0 blocker.

---

## Phase 1 — EyeLab Service (Python WebSocket) (~3–4 days)

**Goal:** standalone Python service running on Windows that consumes JPEG-encoded frames over WebSocket and replies with ArUco-derived poses, reusing all existing webcam-pipeline logic.

### 1.1 Dependencies

- [ ] In `python/requirements.txt`, add `websockets>=12` (server lib) and `python-msgpack` is **not** needed (we use raw binary + JSON).
- [ ] `python -m venv venv && venv\Scripts\activate && pip install -r requirements.txt`.

### 1.2 Frame protocol (`python/frame_protocol.py`)

- [ ] Implement uplink frame parsing:
  - 16-byte header: `magic[4]='ELAB'`, `version=1` (uint8), `reserved[3]=0`, `seqNum` (uint32 little-endian), `timestampNs` (uint64 little-endian).
  - Payload: JPEG bytes.
  - Function: `parse_frame(bytes_msg) → (seq, ts_ns, jpeg_bytes)`.
- [ ] Implement pose response builder:
  - JSON dict: `{ "type": "pose", "seq": int, "timestamp_ns": int, "rvec": [r0,r1,r2], "tvec": [tx,ty,tz], "marker_ids": [int...], "rms_reproj_px": float, "registration_rms_mm": float|null, "latency_ms": float }`.
  - `tvec` in metres (camera frame, OpenCV convention).
  - `null` for `registration_rms_mm` if no full board solve happened on this frame.

### 1.3 Handshake (`python/handshake.py`)

- [ ] Parse incoming `hello`:
  ```json
  { "type": "hello", "client": "<id>", "version": "0.1", "frameWidth": 1280, "frameHeight": 720, "frameFormat": "jpeg-y8", "targetFps": 15 }
  ```
- [ ] Build `ready` reply containing the calibration profile keyed by `client`:
  ```json
  { "type": "ready",
    "calibration": { "fx":..., "fy":..., "cx":..., "cy":..., "k1":..., "k2":..., "p1":..., "p2":..., "k3":..., "imageWidth":..., "imageHeight":... },
    "boardConfig": { "dictionary": "DICT_4X4_50", "markerSizeM": 0.012, "markers": [{"id":0,"position":[x,y,z],"normal":[..],"size":0.012}, ...] }
  }
  ```
- [ ] Look up calibration from `python/calibrations/<client>.yml`. If missing, reply with `{"type":"error","reason":"calibration_missing","client":"<id>"}` and close — Unity will then offer to launch the calibration flow.

### 1.4 Service main loop (`python/eyelab_service.py`)

- [ ] Refactor `webcam_pipeline.py` so its core ArUco-detect-and-solve logic is a function `estimate_pose(jpeg_bytes, calib, board) → PoseResult`. Keep the existing webcam-from-VideoCapture variant working for the standalone CLI.
- [ ] Implement WebSocket server (default `ws://0.0.0.0:8765`). One client at a time; reject second connection with `{"type":"error","reason":"busy"}`.
- [ ] Per-connection state: `(client_id, calib, board, seq_counter, frame_queue depth=2)`.
- [ ] Worker task: pop newest frame from queue, run `estimate_pose`, send pose response. Drop oldest if queue full.
- [ ] Heartbeat: respond to WS pings; close on 15 s of no traffic.
- [ ] Logging: append JSONL to `python/.logs/service_<YYYYMMDD_HHMMSS>.jsonl` — `{ts_recv, ts_sent, latency_ms, seq, marker_count, rms_reproj_px}` per frame.
- [ ] CLI: `python eyelab_service.py --port 8765 --calib-dir ./calibrations`.

### 1.5 Tests (`python/tests/test_handshake.py`)

- [ ] Unit-test `parse_frame` with hand-crafted bytes.
- [ ] Unit-test handshake with fake `hello` strings (valid and invalid).
- [ ] Integration test: spin up service, connect a fake client, send a known-good frame (a JPEG containing a real ArUco marker captured from `webcam_pipeline.py`), assert pose returned.

**Done when:**
- `python eyelab_service.py` starts; you can `wscat -c ws://localhost:8765/aruco`, send a hello, and get a `ready`.
- An end-to-end test: connect, send 30 captured-from-disk JPEG frames, get 30 pose responses with `marker_ids` populated.

---

## Phase 1A — portable Python distribution (~2–3 days; can run in parallel with Phase 2)

**Goal:** ship the EyeLab Service as a download-and-run Windows zip from GitHub Releases. Mirrors the USMA distribution pattern. Any Windows PC, no Python install, no admin, no internet at runtime.

### 1A.1 Bundle layout

```
EyeLab-portable-vX.Y.Z/
├── README.txt                   # plain-text quickstart
├── eyelab_service.bat           # launches the WS service on :8765
├── eyelab_calibrate.bat         # calibration flow (server-side)
├── eyelab_demo_webcam.bat       # standalone OpenCV preview (legacy webcam_pipeline)
├── python/                      # embedded Python 3.11
│   ├── python.exe
│   ├── python311.dll
│   ├── python311._pth           # configured to enable site-packages
│   ├── Lib/site-packages/       # pre-installed wheels (opencv-contrib, numpy, scipy, websockets, pyuff, pyyaml, pillow, pandas, matplotlib)
│   └── Scripts/                 # pip etc.
├── eyelab/                      # project source (subset of python/)
│   ├── eyelab_service.py
│   ├── handshake.py
│   ├── frame_protocol.py
│   ├── webcam_pipeline.py
│   ├── unv_to_json.py
│   ├── pose_estimator.py
│   ├── registration.py
│   ├── camera_utils.py
│   └── eyelab_logger.py
├── calibrations/
│   ├── unity-editor-webcam-default.yml
│   └── unity-android-xreal-eye-default.yml   # filled in Phase 6
├── samples/
│   ├── wireframe.json
│   └── markers/aruco01.png … aruco10.png
└── VERSION                      # plain text e.g. "0.1.0"
```

### 1A.2 Build script `tools/build_portable.py`

- [ ] Download `python-3.11.x-embed-amd64.zip` from `https://www.python.org/ftp/python/3.11.x/`.
- [ ] Extract into `dist/EyeLab-portable-vX.Y.Z/python/`.
- [ ] Edit `python311._pth` to uncomment `import site` (this enables `site-packages/`).
- [ ] Bootstrap pip: download `get-pip.py` from `https://bootstrap.pypa.io/get-pip.py`, run `python\python.exe get-pip.py`.
- [ ] Install requirements into the bundle: `python\python.exe -m pip install -r python/requirements.txt --target python/Lib/site-packages --no-cache-dir`.
- [ ] Copy project sources from `python/*.py` into `dist/.../eyelab/`.
- [ ] Copy default calibration YAMLs into `dist/.../calibrations/`.
- [ ] Copy sample wireframes and markers into `dist/.../samples/`.
- [ ] Write `README.txt` with quickstart and `VERSION` with the tagged version.
- [ ] Write the `.bat` launchers (templates below).
- [ ] Zip `dist/EyeLab-portable-vX.Y.Z/` → `dist/EyeLab-portable-vX.Y.Z.zip`.
- [ ] Print SHA256 for the GitHub Release.

### 1A.3 Launcher templates

`eyelab_service.bat`:
```batch
@echo off
setlocal
set BUNDLE=%~dp0
set PYTHON=%BUNDLE%python\python.exe
set EYELAB=%BUNDLE%eyelab
"%PYTHON%" "%EYELAB%\eyelab_service.py" --port 8765 --calib-dir "%BUNDLE%calibrations"
endlocal
```

(One-line explanation in `README.txt`: "Double-click `eyelab_service.bat`. The service listens on `ws://localhost:8765/aruco`. Connect from Unity Editor or a phone on the same network using `ws://<this-pc-ip>:8765/aruco`.")

### 1A.4 GitHub Release flow

- [ ] Tag a version on `main`: `git tag v0.1.0 && git push --tags`.
- [ ] Run `python tools/build_portable.py --version 0.1.0`.
- [ ] Create a GitHub Release for the tag, attach the zip + a `SHA256SUMS` text file.
- [ ] In `README.md`, add a "Download portable bundle" link to `https://github.com/<org>/<repo>/releases/latest`.

### 1A.5 CI (optional but recommended)

- [ ] Add a `.github/workflows/build-portable.yml` that runs `tools/build_portable.py` on every tag push, attaches the zip to the GitHub Release automatically.

**Done when:** a fresh Windows PC (no Python, no admin) downloads `EyeLab-portable-v0.1.0.zip`, extracts it, double-clicks `eyelab_service.bat`, and a Unity client (Editor or APK) connects to it successfully.

---

## Phase 2 — Unity bridge layer (~2–3 days)

**Goal:** the abstraction that lets us swap C → A → B without touching anything else.

### 2.1 `IArucoPoseBridge.cs`

```csharp
namespace EyeLab.Bridge
{
    public struct PoseResult
    {
        public ulong frameTimestampNs;
        public Vector3 tvec;            // OpenCV camera frame, metres
        public Vector3 rvec;            // Rodrigues vector
        public int[] markerIds;
        public float rmsReprojectionPx;
        public float registrationRmsMm; // -1 if not a full board solve
        public float latencyMs;
    }

    public interface IArucoPoseBridge
    {
        bool IsConnected { get; }
        Task ConnectAsync(BridgeConfig cfg, CancellationToken ct);
        Task<PoseResult?> EstimatePoseAsync(
            ReadOnlyMemory<byte> jpegYBytes,
            int width, int height,
            uint seq,
            ulong frameTimestampNs,
            CancellationToken ct);
        Task DisconnectAsync();
        event Action<CalibrationProfile> OnCalibrationReceived;
        event Action<string> OnConnectionLost;
    }
}
```

### 2.2 `BridgeConfig` (ScriptableObject)

- [ ] Fields: `kind` (enum: WebSocket | Mock | Native | OpenCVForUnity), `wsUrl` ("ws://localhost:8765/aruco"), `clientId` ("auto" → derive from device), `frameWidth`, `frameHeight`, `targetFps`, `connectTimeoutSeconds`, `frameTimeoutMs`.

### 2.3 `WebSocketPoseBridge.cs` (Phase 1's counterpart)

- [ ] Use NativeWebSocket. On `ConnectAsync`: open WS, send `hello` JSON, await `ready` (or `error`), raise `OnCalibrationReceived` with the parsed `CalibrationProfile`.
- [ ] On `EstimatePoseAsync`: pack header + JPEG into a single byte[] of length 16+jpeg.Length; send as binary; await text response with matching `seq`. Resolve. Timeout after `frameTimeoutMs`.
- [ ] Use `XREALMainThreadDispatcher.PerformActionInUpdate(...)` to marshal the result back to the Unity main thread before returning.
- [ ] Reconnect: exponential backoff 250 ms → cap 5 s. Re-handshake.

### 2.4 `MockPoseBridge.cs`

- [ ] Returns a canned pose every call after `frameTimeoutMs / 4` simulated delay. Useful for Editor smoke tests of the rendering pipeline without a camera.
- [ ] Configurable: known `tvec`, `rvec` so the wireframe lands at a predictable place in the scene.

**Done when:** Editor scene `00_Bringup.unity` containing a single `BridgeTester` MonoBehaviour can connect to a running Python service, send a hardcoded JPEG (a `Resources/test_marker.jpg`), and log the returned pose.

---

## Phase 3 — frame capture (~2–3 days)

**Goal:** unify Editor and Android camera sources behind one interface.

### 3.1 `IFrameSource.cs`

```csharp
public struct CameraFrame
{
    public ulong timestampNs;
    public int width;
    public int height;
    public ReadOnlyMemory<byte> grayscaleY; // Y plane / 8-bit grayscale
}

public interface IFrameSource
{
    bool IsRunning { get; }
    Task StartAsync();
    Task StopAsync();
    event Action<CameraFrame> OnFrameAvailable;  // raised on background thread
}
```

### 3.2 `WebCamFrameSource.cs` (Editor / Standalone)

- [ ] Use `UnityEngine.WebCamTexture`, default device, 1280×720.
- [ ] Each `LateUpdate`: if `webcam.didUpdateThisFrame`, copy pixels to a `Texture2D`, get `Color32[]`, convert to single-channel grayscale via `0.299R + 0.587G + 0.114B` (or just take G channel — cheaper, ~equivalent for ArUco).
- [ ] Raise `OnFrameAvailable` with a fresh `byte[]`.

### 3.3 `XREALEyeFrameSource.cs` (Android)

- [ ] Use `XREALRGBCameraTexture.CreateSingleton().StartCapture()`.
- [ ] Each frame: `var yuv = singleton.GetYUVFormatTextures(); var yTex = yuv[0];` — the Y plane is already 8-bit grayscale.
- [ ] Read it back to CPU using `AsyncGPUReadback.Request(yTex, ...)`. Take the callback's `NativeArray<byte>`, copy into a managed `byte[]`, raise `OnFrameAvailable`.
- [ ] Use `XREALPlugin.TryGetRGBCameraFrame(ref ulong)` to get the timestamp at the same Update tick.

### 3.4 JPEG encoding

- [ ] In a `FrameEncoder` static utility: `byte[] EncodeY8AsJpeg(byte[] gray, int w, int h, int quality=85)`.
- [ ] Use Unity's built-in encoder via a temporary `Texture2D` with `TextureFormat.R8`, `EncodeToJPG(quality)`. (Verify `R8` is supported on Android; if not, fall back to a 1-channel `Texture2D` with `RGB24` format using R==G==B==Y, and JPEG-encode that — slightly larger output but works everywhere.)

**Done when:** the Bringup scene picks the right `IFrameSource` automatically (`#if UNITY_EDITOR ... #else ...`) and feeds JPEG frames to the bridge at ~15 fps. End-to-end: webcam moves → Unity logs poses ~15 times/second.

---

## Phase 4 — geometry overlay rendering (~3 days)

**Goal:** display the .unv-derived wireframe at a fixed pose in the scene. No registration yet — just verify the renderer.

### 4.1 `WireframeData.cs`

- [ ] Match the JSON schema produced by `python/unv_to_json.py`:
  ```csharp
  [Serializable] public class WireframeData {
      public Node[] nodes;
      public int[][] traceLines;
      public CoordinateSystem[] coordinateSystems;
      public string units;     // "SI"
  }
  [Serializable] public class Node { public int id; public float x, y, z; public int cs; }
  [Serializable] public class CoordinateSystem { public int id; public float[] matrix; }
  ```

### 4.2 `WireframeLoader.cs`

- [ ] `Task<WireframeData> LoadAsync(string streamingAssetsPath)` using Newtonsoft.Json.

### 4.3 `GeometryRenderer.cs`

- [ ] Instantiate a small sphere prefab at each node (scaled to ~3 mm radius in metres).
- [ ] For each trace polyline (UNV pen-up convention: 0 means break the segment), build a `LineRenderer` configured with width 1–2 mm.
- [ ] Optional: a `TextMeshPro` label as a child of each node, billboarded to the camera, hidden beyond ~1.5 m.

### 4.4 `NodeStatusController.cs`

- [ ] States: `Pending` (white), `Active` (yellow, pulsing), `Hit` (green flash), `Miss` (red flash). Just colours for now — hit logic comes in WP3.

**Done when:** `20_EMA.unity` opens, loads a wireframe.json from `Assets/StreamingAssets/test-wireframe.json`, and renders the wireframe at world origin with all visible nodes.

---

## Phase 5 — pose application & structure anchor (~3–4 days)

**Goal:** place the wireframe at the correct world-frame pose using ArUco poses from the bridge.

**Approach (locked 2026-05-09):** introduce an `IStructureAnchor` interface; v0 implementation is `EwmaStructureAnchor` (option iii from the grill — plain GameObject Transform + EWMA smoothing + outlier rejection). The interface is deliberate: option (i) `ArFoundationStructureAnchor` (uses `XRAnchor` for SLAM-corrected stability) is the planned upgrade if long-session drift is measured to matter. See CONTEXT.md "Resolved decisions" §3.

### 5.1 `HeadPoseHistory.cs`

- [ ] Ring buffer `(ulong timestampNs, Pose worldFromHead)` with capacity 60 (~1 s at 60 Hz).
- [ ] Each `Update`: append `(currentTimeNs, Camera.main.transform pose in world space)`.
- [ ] `Pose Lookup(ulong timestampNs)` — linear search for the entry with smallest `|ts - target|`. Linear interpolation between the two closest is fine (orientation as `Quaternion.Slerp`, position as `Vector3.Lerp`).
- [ ] If `target` is more than 200 ms old, log a warning and return the oldest entry.

### 5.2 `CoordinateConverter.cs`

- [ ] One static class. Single conversion routine (THEORY.md §6.3):
  - `Pose OpenCvToUnity(Vector3 rvecOpenCv, Vector3 tvecOpenCv)`:
    - Build OpenCV → Unity flip via `F = diag(1, -1, 1)`.
    - Convert Rodrigues to a rotation matrix, conjugate by F, extract Quaternion.
    - Negate `tvec.y`.
  - Document the formula in comments. Never duplicate this conversion anywhere else.

### 5.3 `IStructureAnchor.cs` and `EwmaStructureAnchor.cs`

```csharp
public interface IStructureAnchor {
    Pose CurrentPose { get; }                     // world → structure
    Transform AnchorTransform { get; }            // wireframe parents to this
    void OnPoseEstimated(in PoseResult pose);     // called by bridge subscription
    bool IsLocked { get; }                        // true after first stable pose
    event Action OnLocked;
}
```

- [ ] `EwmaStructureAnchor` (the v0 default; chosen 2026-05-09):
  - Hold a `Pose currentAnchor` (the world→structure transform).
  - EWMA smoothing: `α = 0.4` for the first ~10 frames after locking, then `α = 0.15`. Position is straight EWMA; rotation is `Quaternion.Slerp(prev, new, α)`.
  - Outlier rejection: maintain a running stddev of position deltas; reject any new anchor whose delta exceeds `3 × stddev` (with `stddev` floored at 5 mm to handle warmup).
  - On each pose from the bridge:
    1. `headPose = HeadPoseHistory.Lookup(pose.frameTimestampNs)`
    2. `cameraToStructureUnity = CoordinateConverter.OpenCvToUnity(pose.rvec, pose.tvec)`
    3. `worldToStructure = headPose * cameraToStructureUnity` (head-camera offset is identity for v0; a `T_head_camera` multiplies in once SPAAM lands in Phase 8).
    4. Apply outlier rejection; EWMA-update `currentAnchor`; update `AnchorTransform`.
  - Expose `AnchorTransform` as a Transform that the wireframe is parented to.

- [ ] **Future / backup:** `ArFoundationStructureAnchor` — same interface, uses `ARAnchorManager.AddAnchor(...)`. Deferred; tracked in CONTEXT.md "Resolved decisions" §3.

### 5.4 Wire up

- [ ] In `20_EMA.unity`: a single GameObject holds `BridgeRunner` + `FrameSourceRunner` + `HeadPoseHistory` + `StructureAnchor` + the GeometryRenderer.
- [ ] On Awake: connect the bridge, await `OnCalibrationReceived` (the calibration is informational here — only the service uses it), start the frame source.

**Done when:** in Editor with a webcam pointed at a printed ArUco board, the wireframe lands stably on the marker. Move the camera, the wireframe sticks to the marker.

---

## Phase 6 — XREAL Eye calibration UX (~2–3 days)

**Goal:** the service can be told "next N frames are calibration frames" and computes intrinsics + distortion.

### 6.1 Service-side

- [ ] Add a `mode` field to `hello`: `"capture"` (default) or `"calibration"`. In calibration mode, the service:
  - Detects ChArUco corners on each frame.
  - Accumulates frames where ≥4 markers are visible.
  - At each `{"type":"calibration_step","action":"capture"}` request, accepts the current frame.
  - At `{"type":"calibration_step","action":"compute"}`, runs `cv2.aruco.calibrateCameraCharuco` on accumulated corners.
  - Stores YAML at `python/calibrations/<client>.yml`.
  - Replies `{"type":"calibration_done","reproj_rms_px":..., "frameCount":...}`.
- [ ] If reprojection RMS > 1 px, log a warning; UI surfaces this.

### 6.2 Unity-side `CalibrationFlow.cs`

- [ ] In `10_Calibration.unity`: a UI flow that:
  - Connects in `mode: calibration`.
  - Shows operator instructions ("Hold the ChArUco board in front of the camera, tilt it through varied angles").
  - Captures 15–25 frames (button press, or auto when `≥6` markers visible).
  - On "Compute": sends `compute`, displays returned RMS.
  - If successful, prompts to switch to capture mode and run the EMA scene.

### 6.3 ChArUco board

- [ ] Use the existing print already used by the webcam phase. If a fresh print is needed, generate it via `python/generate_markers.py`. Same dictionary as marker board (`DICT_4X4_50`).

**Done when:** the calibration scene produces a YAML that, when used in capture mode, yields ≤3 mm pose error on a known target at 0.5 m.

---

## Phase 7 — recorded-clip regression harness (week 2, ~2 days)

**Goal:** non-flaky CI / pre-commit accuracy gate.

### 7.1 Capture script

- [ ] `python/capture_clip.py`: records a webcam session at fixed fps to `clips/<name>/frame_NNNN.jpg` with a sidecar `ground_truth_poses.csv` (operator types known marker positions, or a precision rig provides them).

### 7.2 Replay harness

- [ ] `python/replay_clip.py --service ws://localhost:8765 --clip clips/labA-trial1`: connects as a fake Unity client, sends frames at 15 fps with the recorded timestamps, collects pose responses, computes per-frame RMS against ground truth, asserts mean < 5 mm and p95 < 8 mm.
- [ ] Wire as a `pytest` test so `pytest -q` runs it. Provide a small reference clip in `python/tests/fixtures/`.

**Done when:** `pytest -q` is green; deliberately injecting a 5 mm bias in the calibration YAML makes it red.

---

## Phase 8 — SPAAM head-camera offset calibration (week 3, ~2 days)

**Goal:** remove the constant overlay offset from the identity head-camera assumption.

- [ ] In `SpaamFlow.cs`: render a small virtual crosshair at a known structure-frame point (e.g. ArUco marker corner). The operator adjusts head position until crosshair visually overlaps the real corner; presses a button.
- [ ] Repeat from 4 different head positions.
- [ ] Solve for the rigid transform `T_head_camera` minimising the residual offset across all 4 alignments.
- [ ] Store as a Unity user-pref or `PlayerPrefs`-stashed pose. `StructureAnchor` multiplies it into the composition: `worldToStructure = headPose * T_head_camera * cameraToStructureUnity`.

**Done when:** running the EMA scene after SPAAM, the wireframe lines visually coincide with the real structure edges to within ~2 mm at 0.5 m.

---

## Acceptance for T2.6 v0 (M2 partial)

End-to-end demo on phone-as-host with XREAL One Pro + Eye:

1. Operator opens the app, connects to the Python service running on the lab PC.
2. (One-time) calibrates the XREAL Eye via the calibration scene.
3. Loads a wireframe.json shipped in StreamingAssets.
4. Sees the wireframe overlay locked to the real test article via ArUco markers.
5. Stable for ≥5 minutes, wireframe RMS error ≤ 5 mm against ground truth.
6. ≥15 fps overlay frame rate sustained.
7. Recovers gracefully when the operator looks away from the markers (SLAM bridges; wireframe drifts but doesn't disappear).

This is M2 from `.docs/PROJECT.md`. T2.6's harder items (impact-hammer hit detection, Testlab COM bridge) are WP3/WP4 and out of scope for v0.

---

## How to use this checklist

- **If handing to an AI agent:** point it at this file plus `CONTEXT.md`, `ADR-001`, and `THEORY.md`. Tell it to work phase-by-phase, committing each phase as a separate PR. Each phase has a "Done when" gate — agent should verify before proceeding.
- **If working manually:** keep this open as a living checklist; tick boxes as you go; capture deviations as comments in this file. If you change an architectural decision, update the relevant ADR or write a new one before the implementation diverges.
- **If you discover the SDK doesn't behave as I described:** your observation wins, my notes lose. Update `CONTEXT.md` "Verified XREAL XR Plugin 3.1.0 surface" and continue.
